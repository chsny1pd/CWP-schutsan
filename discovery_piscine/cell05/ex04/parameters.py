#!/usr/bin/env python3
import sys

num_params = len(sys.argv) - 1
print(f"Number of parameters: {num_params}.")
# para_input = input()
# para = [i.strip('"') for i in para_input.split('" "')]
# print("Number of parameters: ", len(para) if para[0] else 0)
# # "Code Ninja" "Python" "Sad"

